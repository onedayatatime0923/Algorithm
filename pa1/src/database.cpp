
#include <set>
#include "database.h"


void DataBase::compress(){
  set<int, intCompare> wordSet;
  //for (int i = 0;i < _size; ++i){
    //if((iter = wordSet.find(i)) != wordSet.end())
        //u_long f = it; // error here
  }
}

